[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "editdistance"
version = "0.8.1"
description = "Dummy editdistance package to bypass build errors"
authors = [{name = "Antigravity", email = "test@example.com"}]
